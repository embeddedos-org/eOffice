version: '3.8'
services:
  eoffice:
    build: .
    container_name: eos-eoffice
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - EOS_LICENSE_KEY=${EOS_LICENSE_KEY}
    volumes:
      - eoffice_data:/app/data
    networks:
      - eos-enterprise
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  eoffice_data:

networks:
  eos-enterprise:
    external: true
